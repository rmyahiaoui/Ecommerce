<%= form_for(@categori) do |f| %>
  <% if @categori.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categori.errors.count, "error") %> prohibited this categori from being saved:</h2>

      <ul>
      <% @categori.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
